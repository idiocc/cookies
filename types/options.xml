<types namespace="_goa">
  <import from="http" name="IncomingMessage" desc="The client request."/>
  <import from="http" name="ServerResponse" desc="The server response."/>

  <type record name="CookiesOptions" desc="Options for the constructor.">
    <prop opt type="!(Array<string>|_goa.Keygrip)" name="keys">
      The array of keys, or the `Keygrip` object.
    </prop>
    <prop opt type="boolean" name="secure">
      Explicitly specifies if the connection is secure, rather than this module examining request.
    </prop>
  </type>
</types>